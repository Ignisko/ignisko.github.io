import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://spol.ski/', // For local development or https://github.com/Ignisko/ignisko.github.io
});
